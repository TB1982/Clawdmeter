# PlatformIO build artifacts
firmware/.pio/
firmware/.vscode/

# OS
.DS_Store

# Claude Code — session state, worktrees and local settings stay local. Skills
# are the exception: a skill is a written-down way of working on this repo, so
# it belongs to the repo the same way tools/ and docs/ do.
#
# The first line is not redundant. A global core.excludesFile may carry a blanket
# `.claude/`, and git will not re-include anything whose parent directory is
# excluded — so the directory has to be un-excluded before its contents can be
# selectively let back in. Repo .gitignore outranks the global file, so this
# works without touching anyone's machine config.
!.claude/
.claude/*
!.claude/skills/

# Node tooling deps (npm install --no-save)
tools/node_modules/
tools/package-lock.json

# Contact sheets from tools/preview_anim.js — regenerate, don't commit
tools/preview/

# Official Clawd art imported by tools/import_official.js. Not committed for
# two reasons: it is Anthropic's, and it is one command away from upstream's
# own copy, so committing it would be redistributing art to save nothing.
# Edited derivatives belong in tools/drawn_anims/ like any other drawing.
tools/official_anims/

# macOS makes " 2"/" 3" copies of files when iCloud syncs a directory that git
# is writing to, and Documents/ is synced by default. They are untracked, they
# are stale, and PlatformIO compiles them — 64 duplicate symbols at link time.
*\ [0-9].*

# Mac daemon venv
daemon/.venv/

# Python bytecode cache
__pycache__/
*.pyc
